[build]
  # Railway uses the official n8n Docker image — no custom Dockerfile needed
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "n8n start"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
